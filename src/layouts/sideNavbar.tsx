import NavLinks from "../components/navLinks";

const SideNavbar = () => {
    return (  
        <div className="side-bar">
            <NavLinks />
        </div>
    );
}
 
export default SideNavbar;